import React from 'react'
// import { useSelector } from 'react-redux';

export default function Cart() {
  // const {loading }= useSelector((state) => state.userToken)
  //   console.log(loading);
  return (
    <div>Cart</div>
  )
}
